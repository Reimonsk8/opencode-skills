---
description: Bootstrap the adversarial agent harness (roles, adversaries, workflows, protocols, quality bar, AGENTS.md) for the current repository, adapted to its real stack. $ARGUMENTS = optional project goal.
agent: build
---

Run the `agent-harness` skill for this repository: load it, follow every step, and CREATE the files — do not merely describe them.

Project goal (optional): $ARGUMENTS

After generating, validate per the skill's checklist before reporting done. Report: files created, commands detected, workflow to invoke, verification performed.