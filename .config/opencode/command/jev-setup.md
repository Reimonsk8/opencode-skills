---
description: Check JEV status, install / on / off / update / test TypeSafe Jev.
agent: build
---

Follow the `jev-setup` skill: check status (typesafe-ai skill PRESENT/MISSING, TYPESAFE_API_KEY SET/MISSING + persistent, typesafe-sdk version, opencode skills.paths, API ping returns answers), report as table, then ask what to do (Install / Turn on / Turn off / Update / Test / Nothing). Execute only the pick, then re-run the status check.
